name: Greetings

on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@e6e5d6e0e2ffa50e1399c49a15f7c0e2b8949d4e # v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: |
          👋 Thanks for opening your first issue! Please provide as much context as
          possible. A maintainer will follow up shortly.
        pr-message: |
          👋 Thanks for your first pull request! Please make sure your changes follow
          our contributing guidelines. We'll review it as soon as we can.
